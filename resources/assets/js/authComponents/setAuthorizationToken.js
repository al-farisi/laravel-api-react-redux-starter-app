// import axios from 'axios';
//
// export default function setAuthorizationToken(token, refresh = false) {
// 	if (token) {
// 		refresh && delete axios.defaults.headers.common['Authorization'];
// 		axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;
// 	} else {
// 		delete axios.defaults.headers.common['Authorization'];
// 	}
// }
